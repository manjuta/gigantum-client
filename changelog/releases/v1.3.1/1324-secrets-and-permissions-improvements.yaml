prLink: "https://github.com/gigantum/gigantum-client/pull/1324"
changelog:
  - type: IMPROVEMENT
    description: With new bases, git will work on the Project from inside the container, including LFS and custom certs
  - type: FIX
    description: Secrets are now owned by the Project user, and have restrictive permissions (suitable for SSH, etc.)
